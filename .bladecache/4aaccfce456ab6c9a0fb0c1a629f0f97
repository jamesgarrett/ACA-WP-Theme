<ul class="list-block">
	<?php $bladequery = new WP_Query(array('post_type' => 'flavors')); if ( $bladequery->have_posts() ) : while ( $bladequery->have_posts() ) : $bladequery->the_post(); ?> 
	<a title="<?php echo the_title(); ?>" href="<?php echo the_permalink(); ?>">
	
		<? jsl(get_field("flavor_illustration",$post->id)); ?>
	
		<li class="clfx" data-swatch="<?php echo  get_field('flavor_swatch'); ; ?>">
			<aside class="img-attachment">
				<img src="<?php echo  get_field("flavor_illustration",$post->id)["url"] ; ?>" alt="<?php echo the_title(); ?>">
			</aside>
			<h1 class="fl-l txt-display"><?php echo the_title(); ?></h1>
		</li>
		
	</a>
	<?php endwhile; ?><?php else: ?>
		<div class="bgc-err"><?php echo  __('This is a flavorless world!') ; ?></div>
	<?php endif; wp_reset_postdata(); ?>
</ul>